-- =====================================================================
-- 지급회의서 지출일자 — 결재일(decided_at)과 실제 지출한 날짜가 다를 수 있어(예: 지난주
-- 영수증을 이번 주에 결재) 별도 필드로 둔다. 기안문/시행문은 결재일을 그대로 시행일로 쓰므로
-- 별도 필드가 필요 없다.
-- =====================================================================

ALTER TABLE documents ADD COLUMN IF NOT EXISTS expense_date DATE;
COMMENT ON COLUMN documents.expense_date IS '지급회의서 전용 — 실제 지출(집행)한 날짜. 결재일(decided_at)과 다를 수 있음';
