# 변경 이력

버전 관리 기준은 [VERSIONING.md](VERSIONING.md) 참고 — `1.0.0` 전까지는 전부 초안 단계.

## v1.0.0 (2026-08-14)

**최초 완성판.** 토스 디자인 가이드 기반 내부 디자인 전면 개편([VERSIONING.md](VERSIONING.md) 기준 — "이 정도면 회사 회계 업무를 이걸로 돌려도 된다"고 판단해 v1.0.0으로 확정). 기능 로직은 전혀 건드리지 않고 `src/style.css`와 각 페이지의 프레젠테이션 마크업만 개편했다.

- **컬러·타이포그래피 전면 교체**: 토스 실제 브랜드 블루(#3182F6)와 OKLCH 기반 색 체계 원칙을 참고해 팔레트를 새로 짬. 헤더는 네이비 배경 → 흰 배경+헤어라인, 카드는 테두리 위주 → 부드러운 그림자 기반, 테이블은 진한 헤더 → 옅은 회색+보더리스, 폼은 아웃라인 → 채워진(filled) 입력창으로 전환
- **버튼·표·폼 정렬/균형 버그 다수 수정**: 표 셀에 버튼 두 개가 간격 없이 붙던 문제, "비활성화" 버튼이 목록 전체에서 danger 빨간색으로 시각적으로 과했던 문제, 라벨 없는 버튼이 옆 입력창과 세로로 안 맞던 문제(잔액대사·소득금액조정합계표·기납부세액 3곳), `<h3>` 하위 제목에 스타일이 아예 없어 브라우저 기본값으로 깨지던 문제, 표 "합계" 행 스타일이 5곳에서 다른 표들과 다르게 나오던 문제 등을 실제 로그인 화면에서 스크린샷으로 대조하며 수정
- **입력 요소 스타일링 구조 개선**: 분개입력 화면의 계정 선택 행(`.line-row`)과 거래 검토 화면의 계정 선택 셀에 select/input 스타일이 통째로 빠져 있던 것을 발견 — 컨테이너마다 스타일을 따로 추가하는 대신 모든 select·input에 적용되는 전역 기본 스타일로 구조를 바꿔, 이런 "이 화면만 빠뜨림" 버그가 구조적으로 재발하지 않도록 함. 체크박스·파일 업로드 입력도 브랜드 컬러로 통일
- **PWA 아이콘·테마컬러 정비**: 매니페스트·파비콘 테마컬러를 새 흰색 헤더에 맞춰 갱신
- 남겨둔 항목: 좁은 화면(~740px)에서 열이 많은 표가 텍스트 줄바꿈으로 지저분해지는 문제 — 데스크톱에서는 문제없고 표마다 판단이 달라 이번 범위에서는 제외

## v0.2.1 (2026-07-29)

- **앱 아이콘 교체**: 기존 네이비 배경 + 흰 원만 있던 밋밋한 아이콘을 장부 라인 모티프(노트 + 가로줄)로 교체(192px·512px). 파비콘 링크(`<link rel="icon">`)가 아예 없던 것도 이번에 추가

## v0.2.0 (2026-07-29)

대시보드 신규 구현 + 증권관리 화면 정합성 정비. 증권 자동분개·재무제표·증권관리 기본 골격이 갖춰진 묶음이라 마이너 버전을 올림([VERSIONING.md](VERSIONING.md) 기준).

- **대시보드 신규 구현**: 로그인 후 첫 화면으로, 자산총계·현금성자산·증권평가금액·당기순이익 KPI 4종과 반기별 자산 구성(부채+자본 누적막대, 최근 3개년) 차트를 보여줌. "계좌별 현금비중"은 국민은행·키움2843·키움2845 실물계좌 3개가 전부 같은 GL계정(11101)에 연결돼 있어 원장만으로는 분리가 안 된다는 걸 확인하고, 억지로 계산하는 대신 그 이유를 화면에 각주로 남김(계좌관리 파이프라인이 더 갖춰지면 추가 예정)
- **증권관리에 [환율(거래일 대사)] 탭 신규 추가**: 달러 매수·매도·배당 거래가 있었던 날짜마다 fx_rates가 실제로 커버되는지(정확/근사/미확보) 보여주는 대사 화면. 처음엔 "환전 거래"로 오해할 수 있는 이름이라 "환율(거래일 대사)"로 명확히 함
- **[버그 수정] 2026년 상반기(1~6월) 매매·배당이 매매내역/배당금내역/환율 화면에서 통째로 빠져 있던 문제**: 반기 손익 재계산 작업 때 계산 결과만 전표로 직접 반영하고 `securities_transactions`는 건드리지 않아서 생긴 공백. 매수 35건·매도 11건·배당 6건(총 52건)을 조회 전용(`status='ignored'`)으로 백필 — 수량은 2026-06-30 잔고증명서 재파싱 결과와 대조해 검증 완료(2843·2845 각각 확인 가능한 7개 종목 전부 정확히 일치). 이 공백은 표시 문제일 뿐 아니라, 해당 기간 원본 파일을 나중에 실수로 재업로드해도 dedup_key가 없어 중복 분개될 위험이 있었는데 이번 백필로 막힘
- **[버그 수정] 매매내역 화면에 연도별 조회 기능 누락**: 배당금내역·환전내역·환율 3개 화면엔 이미 있던 연도 선택 기능이 매매내역에는 없었음 — 같은 패턴으로 추가

## v0.1.10 (2026-07-28)

- **[버그 수정] 증권 재평가 화면의 잔고증명서 PDF 자동인식이 종목명이 여러 줄로 쪼개진 경우 이름을 잘못 붙이던 문제**: 종목명이 길어 셀 안에서 줄바꿈되면(예: "미국 채권 아이셰어즈" / (데이터 행) / "ETF") 데이터 행 자체에는 이름이 비어 종목이 통째로 누락되는 문제가 있었음. 앞뒤 텍스트 조각을 모아 붙이도록 고쳤으나, 그 과정에서 이미 이름이 온전한(줄바꿈 없는) 종목까지 다음 종목의 이름 앞부분을 잘못 흡수하는 회귀가 생겨 재수정(`kiwoomBalanceCert.js`) — 2025년말 2845계좌(AGG·IAU·IVV 3종목, 줄바꿈 있음)와 2026년 상반기 2843·2845계좌(10종목, 줄바꿈 없음) 양쪽 다 재검증 완료

## v0.1.9 (2026-07-28)

**2026년 상반기 금융영업손익 재계산 + 배당 취소거래 이중계상 버그 수정** — 사용자가 "코인베이스 매도 손실이 반영 안 된 것 같다"고 지적한 것을 계기로 조사하다가, 반기 결산에 쓰인 "매도실현손익 및 배당금수익(순)" 8,900,983원 단일 합계 전표가 실제로는 종목별로 검증 불가능했고, 그 근거 자료(원본 PDF 거래내역 · DB 저장 전표 · CPA 반기 워크시트)가 서로 정확히 일치하지 않는다는 걸 발견.

- **[버그 수정] 배당 취소거래 이중계상**: 키움 원본 데이터에 배당이 "최초입금 → 취소출금(같은 금액) → 정정입금"으로 여러 줄에 걸쳐 나오는데, 적요에 전부 "배당"이 들어있어 취소출금도 배당으로 잘못 더해지고 있었음(2026-07-08 배당 4건이 실제 3.28달러인데 9.90달러로 3배 가까이 과대계상됨). `kiwoomPdf.js`는 입출구분으로, `kiwoomCsv.js`는 "취소" 키워드로 부호를 반대로 처리하도록 수정. `buildDividendLines`도 음수(취소) 순액을 올바르게 차변/대변 반대로 처리하도록 보강
- **금융영업수익 계정 분리**: 41002를 "증권매매수익"으로 개칭하고 41003 "배당금수익"을 신설(51002는 "증권매매손실"로 개칭) — 손익계산서에 상품매출손익/금융영업손익 구분 표시도 함께 추가
- **2026년 상반기 매도손익 종목별 재계산**: 원본 거래내역 9개 종목(AXP·테슬라·아이온큐·SOXL·코인베이스·미국채권아이셰어즈ETF·QQQ레버리지ETF) 전부를 처음부터 이동평균으로 재구성해 실제 손익을 검증 — 기존 8,900,983원(반기 합계, 검증 불가)을 8,359,945원(종목별 재계산, 손익계산서에 종목별 전표로 반영)으로 정정. 기존 개별 매도 전표(원가 제거분)와 로트 상태는 그대로 두고 손익 인식 부분만 종목별로 교체(재무상태표·보유종목 대사 영향 없음)
- 2025년 환전내역 백필 시 채워둔 fx_rates 공백 마저 보강(2026년 상반기 배당·매도 관련 신규 20여 건)

## v0.1.8 (2026-07-28)

- **[버그 수정] 분개장에 전표번호가 안 보이던 문제**: `entry_no` 컬럼은 스키마에 있었지만 레거시 이관(2025년 100건) 이후로는 어떤 화면도 값을 채우지 않아 전부 공란이었음 — 수기 분개·거래 검토·증권 거래 분개·증권 재평가, 분개를 생성하는 4곳 전부에 회계연도별 5자리 순번 자동 채번(`fetchMaxEntryNo`/`formatEntryNo`, `src/lib/data.js`) 추가. 기존에 번호 없던 29건도 소급 채번(2026년 00067~00095)

## v0.1.7 (2026-07-28)

- **2025년 매매내역 백필**: [매매내역] 화면도 환전·배당과 같은 원인(2025년은 자동분개 파이프라인 이전에 세무사 요약 방식으로 이관돼 `securities_transactions`에 건별 기록이 없었음)으로 2026년분만 보이던 문제 — 원본 종합거래내역 PDF에서 매수·매도 90건(2843 75건·2845 15건)을 조회 전용으로 백필(`status='ignored'`, 분개는 이미 요약 형태로 확정돼 있어 새로 만들지 않음). 상태 배지에 "과거 이관분" 구분 추가(매매내역·배당금내역 공통)

## v0.1.6 (2026-07-28)

메뉴 재검토 + 기능 완전성 점검 후 iOS 실사용 중 발견된 문제 4건 수정 + 현금흐름표 신규 구현.

- **메뉴 순서 원칙 보강**: 그룹 "내부" 정렬 기준(처리형 그룹=업무 처리 순서 / 리포트형 그룹=요약→상세)을 명문화하고, 자동분개 그룹의 "증권 재평가"를 "잔액 대사" 뒤로 이동(주기적 마감 작업은 일상 거래 처리·대사가 끝난 뒤에 오는 게 논리적)
- **[버그 수정] 계정별원장이 회계연도를 선택해도 그 이전 전체 이력을 다 보여주던 문제**: 실질계정(자산·부채·자본)도 이제 "전기이월" 한 줄 + 선택 연도 거래만 표시(시산표·재무제표는 기존 누적 로직 그대로 유지, 원장 화면 표시만 수정)
- **증권 재평가 기준일을 반기말(6/30)·연말(12/31)로 제한**: 실제 잔고증명서 없이 임의 날짜로 재평가 분개가 만들어지는 것을 방지. 다른 재무제표 화면과 같은 시각적 패턴(연도 선택 + 퀵버튼)의 전용 선택기(`halfYearEndPickerHtml`) 신규 추가
- **현금흐름표(간접법) 신규 구현** — 재무제표 5종 중 마지막 한 장. 매도가능증권 매매는 이 회사 실질(증권투자가 사실상 주업)에 맞춰 영업활동으로 분류(K-GAAP 일반 관행은 투자활동). 개발 중 실제로 발견한 버그 2건을 수정해서 정확도를 확보: (1) 매도대금 전액을 현금흐름 조정에 더하면 당기순이익에 이미 반영된 실현손익이 이중계상되는 문제 → 원가 회수분만 반영하도록 수정했다가, (2) 그마저도 적요 태그(`[증권매수]`/`[증권매도]`) 매칭 방식이 파이프라인 완성 전 수기 입력된 거래를 놓치는 것을 발견해, 태그에 의존하지 않는 잔액 기반 공식(Δ매도가능증권 − Δ매도가능증권평가익)으로 재설계 — 2025·2026 각각 기말현금이 실제 보통예금 잔액과 정확히 일치함을 검증
- **2025년 환전내역 백필**: [환전내역] 화면이 2026년(자동분개 파이프라인 가동 이후) 것만 보이던 문제 — 2025년 원본 종합거래내역 PDF(2843·2845)를 다시 확보해 개별 환전 이벤트 39건을 조회 전용으로 반영(분개는 이미 세무사 요약 방식으로 확정돼 있어 새로 만들지 않음, `status='ignored'`로 저장해 거래 검토 화면에는 안 나타남)
- **[버그 수정] 보유종목 현황의 2026-06-30 취득원가 대사 오차(38,593원) 재발**: 예전 세션에서 `securities_lots`(실시간 로트)에는 비례배분 보정을 적용했는데, 정작 대사 화면이 실제로 참조하는 `securities_valuations`(그 시점 스냅샷 테이블)에는 보정이 반영되지 않아 미보정 값이 그대로 남아있었음 — 스냅샷에도 동일하게 비례배분해 "일치 ✓"로 복원
- **2025년 배당금 백필**: [배당금내역]에 연도 선택 + 연간 합계 추가(배당은 자산·부채와 달리 매년 0부터 새로 집계되는 항목이라 전기이월 개념 없이 연도 단위로 끊어서 봄). 2025년분도 원본 PDF에서 56건 백필(조회 전용, `status='ignored'`)
- **증권 재평가 화면에 잔고증명서 PDF 업로드 자동 인식 추가**: 키움증권 "잔고증명서"(보유 증권 명세서, 2페이지)를 올리면 종목별 평가금액이 자동으로 채워짐(수기 입력도 계속 가능). "종합 거래내역 조회"(거래이력, 기존 파서)와 컬럼 구성이 완전히 다른 별도 문서라 신규 파서(`kiwoomBalanceCert.js`) 작성 — 종목코드가 없는 문서라 종목명을 공백 무시하고 매칭, 발급 시기별로 다른 두 가지 PDF 레이아웃 변형(2025년말 발급분은 평가단가 소수점이 셀 줄바꿈으로 토큰이 쪼개짐)을 실제 파일 4건(2025년말·2026년상반기 × 2843·2845)으로 검증
- **UI 통일**: 화면마다 제각각이던 툴바 내 select/date input/버튼 크기·폰트를 34px 높이·13px 폰트로 통일

## v0.1.5 (2026-07-27)

**증권 매도 시 유보(OCI) 자동 추인** — 매도가능증권 반기 재평가로 자본(33001)에 쌓인 평가이익 중 매도 종목분이 회계·세무 양쪽에 영구히 남던 문제 수정.

- `securities_lots`에 종목별 `fair_value`/`unrealized_oci` 채워넣기 시작(06/30 잔고증명서 단가 기준 근사 백필, 분개·세무조정 미발생 — 기존 집계 유보와 이중계상 방지)
- 매도 분개에 종목별 OCI 추인 줄 자동 추가(`차)매도가능증권평가익 / 대)매도가능증권`, 매도수량 비례) + `유보 관리`에 종목명 붙은 세무조정 페어 자동 등록 — 손익 미영향, 과세표준 불변 확인
- **신규 화면 [증권 재평가]**(자동분개 그룹) — 앞으로 반기 마감 때 종목별 평가금액만 입력하면 재평가 분개·세무조정·로트 갱신이 자동 처리됨(기존엔 수기 SQL로 처리)
- 실제 매도 테스트(IONQ 5주)로 5줄 분개·로트 갱신·유보 반영·과세표준 불변 전부 검증 후 테스트 데이터 롤백 완료

## v0.1.4 (2026-07-27)

메뉴 배치 원칙 확립: 기초정보→자동분개→전표·장부→재무제표→법인세(범용 회계 사이클, 데이터 흐름 순서) 뒤에
증권관리(투자 부문 전용 분석 모듈)를 배치. 증권관리를 맨 뒤로 이동.

## v0.1.3 (2026-07-27)

- **국민은행 PDF 파서 추가**(`kbBankPdf.js`) — "계좌 거래내역 조회" 형식 PDF 지원(기존 `.xls`에 더해). 실제 2026년 상반기 파일로 검증(13건, 입출금 합계가 PDF 원본 헤더와 정확히 일치)
- **2025년 법인세 납부 분개 반영**: 국민은행 실제 출금 내역(2026-03-31, 법인세 46,770원 + 지방소득세 4,670원 — 신고서 금액과 8원 차이는 신고서 반올림으로 추정, 실제 출금액 기준으로 계상)을 발견해 분개 등록. **법인세법 §21에 따라 손금불산입(기타사외유출) 세무조정도 함께 등록** — 안 하면 과세표준이 과소계상됨을 확인
- 해외배당 원천세 처리를 **순액법**으로 확정(2025년 세무사 실제 처리 방식과 일치, 법인세법 §57 손금산입법과 동일 결과) — 미국 원천세율(15%)이 현재 한계세율보다 높아 세액공제 실익이 없기 때문. 연간 과세표준이 약 5억원(평균세율 15% 도달)을 넘으면 총액법(세액공제법) 전환 검토

## v0.1.2 (2026-07-27)

해외주식 배당 원천세 회계처리 방식 확정 및 관련 버그 수정.

- **해외배당 원천세 = 순액법으로 확정**(법인세법 §57 손금산입법과 동일 결과). 2025년 세무사 실제 처리 방식과 일치시킴 — 미국 원천세율(15%)이 현재 법인세 한계세율(9~19%)보다 높아 세액공제 한도에 걸려 실익이 없기 때문. 국내(원화) 배당·이자 원천징수는 기존대로 선납세금 자산 계상 유지. **전환 기준을 코드 주석에 명시**: 회사 전체 연간 과세표준이 약 5억원(평균세율 15% 도달 지점)을 넘으면 총액법(세액공제법)으로 전환 검토
- 위 결정 전에 이미 총액법으로 생성·전기했던 2845 배당 4건을 `void` 처리하고 순액법으로 재생성, 잘못 등록됐던 외국납부세액 공제 신청分도 삭제
- **[버그 수정] 세무 화면 3곳이 취소(void)된 분개를 계속 집계**: `taxCredits.js`/`taxAdjustments.js`/`securitiesPositions.js`의 GL 조회가 `journal_entries.status`를 필터링하지 않아, 방금 위 정정에서처럼 분개를 취소해도 세무 대사·유보 검증·보유종목 대사에 계속 잡히는 문제 — 위 3곳 모두 `status='posted'` 필터 추가(재무제표 쪽 `fetchTrialBalance`는 원래부터 정상)

## v0.1.1 (2026-07-27)

2026년 7월 반기 마감 + 실사용 검증 중 발견한 버그 수정.

- **2026년 7월 거래 반영**: 2843 계좌 현금 3건, 2845 계좌 증권거래 5건(배당 4·매수 1) 분개 생성 및 전기 완료
- **테스트 잔재 데이터 삭제**, **선납세금 자동분류 매핑 규칙 추가**(`예탁금이용료이자세금출금` → 선납세금)
- **[버그 수정] 이동평균 로트 티커 불일치**: 개시 로트는 영문 티커(TSLA 등)로, 파서는 한글 종목명을 티커로 저장해 같은 종목이 별도 로트로 쪼개지는 문제 — 전체 로트를 한글 종목명 기준으로 통일해 해결
- **[버그 수정] 키움 PDF 형식 판별 가드 우회**: "잔고 및 거래 명세서" 형식을 걸러내는 조건이 블랙리스트 방식(`!includes('종합')`)이었는데, 계좌 유형 표기 "위탁**종합**"에 우연히 "종합"이 포함돼 가드가 뚫리는 것을 실사용 검증 중 발견 — 화이트리스트 방식으로 교체
- **기납부세액 자동 제안**: 선납세금 장부와 등록 내역 간 차이를 적요 기반으로 자동 분류해 제안하는 배너 추가(국내 원천징수=원천납부, 해외 배당=외국납부세액+한도계산용 국외원천소득 자동 산출)
- 국민은행 `.xls`, 키움 PDF 파이프라인 실제 브라우저 업로드 경로 검증 완료

## v0.1.0 (2026-07-27)

최초 버전 관리 시작. 이 시점까지 만들어진 것:

- 기초정보·전표·장부·재무제표 기본 골격 (계정과목, 분개 입력·분개장·계정별원장·시산표, 재무상태표·손익계산서·이익잉여금처분계산서·자본변동표)
- Vercel 배포 + PWA
- 자동 분개 파이프라인 (계좌 관리·거래 업로드·거래 검토·분개·잔액 대사) — 국민은행 `.xls`, 키움증권 CSV/PDF 파서
- 증권 매매·배당 자동분개 (이동평균법, 거래 즉시 손익 인식) — 증권관리(보유종목 현황·매매내역·배당금내역·환전내역) / 자동분개(증권 거래 분개) 메뉴 분리
- 개시 로트 복원(2026-07-01 기준, 잔고증명서 대사 완료)
- 법인세 모듈 4종 (법인세 계산·소득금액조정합계표·유보 관리·기납부세액과 세액공제) — 2025년 실제 신고서와 대조 검증 완료

자세한 배경과 결정 이유는 [PROJECT_HISTORY.md](PROJECT_HISTORY.md) 참고.
