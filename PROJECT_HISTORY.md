# 프로젝트 이력 — 주식회사 프래프트 관리회계 시스템

이 문서는 이 프로젝트가 어떤 순서로, 왜 그렇게 만들어졌는지 기록한다. 코드는 "무엇을" 하는지 보여주지만 "왜 그 방식을 택했는지"는 대화 기록에만 남기 쉬워서, 나중에 이 앱을 다시 열어볼 때를 위해 별도로 정리해둔다.

## 0. 배경

주식회사 프래프트(투자·상거래 겸업 법인)가 세무사에게 맡기던 회계 처리를 자체 시스템으로 대체하기 위해 시작한 프로젝트. Vite + 순수 자바스크립트(프레임워크 없음) + Supabase(Postgres/Auth) 구조.

## 1. 기초 골격

계정과목(복식부기 트리 구조), 분개 입력, 분개장, 계정별원장, 합계잔액시산표, 재무제표(재무상태표·손익계산서·이익잉여금처분계산서·자본변동표) — 표준 회계 프로그램(더존 등)의 메뉴 관례를 따라 **기초정보 / 전표·장부 / 재무제표** 3단 구조로 시작했다.

핵심 설계 하나: **마감분개 없이 실시간 당기순이익을 계산**하는 구조를 택했다. 수익·비용(명목계정)은 조회 시점에 회계연도 시작일부터 다시 집계하고, 자산·부채·자본(실질계정)은 전체 누적으로 잡는다(`fetchTrialBalance`, `src/lib/data.js`). 이 때문에 "누적순이익"(과거 완료 연도의 이익)이 연도 경계를 넘을 때 사라지는 버그가 여러 화면(시산표·재무상태표·이익잉여금처분계산서·자본변동표·원장)에서 반복적으로 발생해, 매번 같은 패턴(실질계정 vs 명목계정 하한 필터링)으로 고쳐야 했다.

## 2. 배포 및 모바일 접근

로컬에서만 돌던 앱을 아이폰 등 외부에서도 쓸 수 있게 Vercel에 배포. Capacitor로 네이티브 앱을 만드는 대신 **PWA**(매니페스트 + 서비스워커, 설치 가능·오프라인 셸)로 가는 것을 사용자가 명시적으로 선택. 재무제표·장부 화면에 엑셀 다운로드 추가, 분개입력 화면의 고정 7열 그리드를 모바일 대응으로 수정.

## 3. 자동 분개 파이프라인 — 1차 (은행 현금)

**목표**: 키움증권 계좌 2개(2843·2845) + 국민은행 계좌 1개의 거래내역 파일을 업로드하면 자동으로 분개 초안을 만들어주는 기능.

구현: 계좌 관리(`financialAccounts.js`) → 거래 업로드(`importTransactions.js`, 파일 파싱·중복제거) → 거래 검토·분개(`reviewTransactions.js`, 적요 패턴 학습으로 상대계정 제안) → 잔액 대사(`balanceSnapshots.js`, 잔고증명서와 대조). 파서는 국민은행 `.xls`(`kbBank.js`)와 키움 PDF(`kiwoomPdf.js`, pdfjs-dist로 좌표 기반 텍스트 추출) 두 가지로 시작.

이 1차 버전은 **원화 예수금잔고가 실제로 바뀌는 거래(환전·이자·세금 등)만** 자동화했고, 매수·매도·배당은 의도적으로 걸러냈다. 이유: 증권 거래는 원가·손익 계산이 필요해 단순 현금흐름 로직으로는 처리할 수 없었기 때문.

이어서 사용자가 "키움 거래내역을 월중에는 CSV로도 받을 수 있다"고 알려줘 CSV 파서(`kiwoomCsv.js`)도 추가.

## 4. 증권 매매 자동분개 — 핵심 기능으로 재정의

사용자가 명확히 못박은 지점: **"자동분개의 핵심기능이 매도가능증권 거래와 세금·수수료 등 부수거래를 반영하는 것이고, 이걸 구현 못하면 이 프로젝트는 실패"**. 최우선 순위로 전환.

### 4-1. 기존 세무사 처리 방식 조사

사용자가 제공한 세무사의 실제 2025년 결산 파일(`거래내역정리(2025)_이동평균.xlsx`)과 이미 이관된 FY2025 100건 분개를 대조해서 알아낸 것:
- **원가법**: 이동평균법 — 매수 시마다 (기존원가+신규매입원가)÷총수량으로 평균단가 갱신
- **환율**: 원본엔 없고 세무사가 하나은행 과거환율조회에서 날짜별로 수동 입력(환율참조표 시트에 2025년 101일치)
- **손익 인식**: 세무사는 매수·매도를 원가로만 상계하고, 손익은 12/31에 연간 합계로 한 번에 인식(`매도실현수익`/`배당금수익`/`매도수수료` 분개)

### 4-2. 이 프로젝트에서 바꾸기로 한 것

- **손익 인식 시점을 거래 즉시로 변경** — 세무사의 연말 일괄 인식 방식 대신, 매도 즉시 3줄 분개(차)보통예금 매도대금 / 대)매도가능증권 이동평균원가 / 차·대 금융영업비용·수익 차액)로 처리. "실시간 재무 파악"이 이 앱의 목적이기 때문.
- **수수료 처리**: 매수 시 취득원가에 자본화, 매도 시 매도대금에서 차감 — K-GAAP 원칙대로. 세무사의 "연말에 지급수수료로 일괄 계상" 방식보다 이론적으로 정확함.
- **환율 확보 방식**: 사용자가 수동 업로드 방식과 앱이 브라우저에서 직접 외부 API를 호출하는 방식(CORS 불안정) 둘 다 거부하고, **"내가 전에 찾아서 반영한 것처럼 외부에서 찾아줘야 한다"**고 지정 — 즉 Claude가 매 결산 시점마다 WebSearch/WebFetch로 실제 환율을 조회해 `fx_rates` 테이블에 채워넣는 방식으로 확정.

### 4-3. 메뉴 구조를 둘러싼 3차례 수정

1차 계획은 증권 장부 화면에 분개생성 버튼을 같이 두는 것이었으나, 사용자가 두 차례에 걸쳐 계획을 반려하며 구조를 바꿈:
- **"증권관리"라는 별도 최상위 메뉴 그룹을 신설**하는 게 더 효율적인지 검토해보라는 요청 → 신설로 결정.
- **"증권관리는 조회·분석 전용이어야 하고, 분개생성 액션은 전부 '자동분개' 그룹에 있어야 한다"**는 지적 — "증권관리 메뉴에서 증권 관련 분개를 하는 기능을 넣는건 메뉴 기획의 실패"라는 표현으로 명확히 함. 이유: 향후 수익률분석·리밸런싱 같은 순수 분석 기능을 증권관리에 얹을 것인데, 회계처리 액션과 섞이면 안 된다는 것.
- **"계좌 관리"는 계정과목과 같은 성격(자주 안 건드리는 마스터데이터)이니 "기초정보" 그룹으로 이동**.

최종 구조: 기초정보(계정과목·계좌 관리) / 자동분개(거래 업로드·거래 검토·분개·**증권 거래 분개**·잔액 대사) / 전표·장부 / **증권관리**(보유종목 현황·매매내역·배당금내역·환전내역, 조회 전용) / 재무제표.

### 4-4. 구현과 실측 검증

`securities_lots`(기존에 스키마엔 있었지만 미사용이던 테이블)를 종목당 이동평균 포지션 레코드로 재해석. `securitiesJournal.js`에 순수 계산 함수(`computeBuy`/`computeSell`/`computeDividend`)를 만들고, `securitiesReview.js`(자동분개 그룹, 날짜순 처리 강제)에서 분개를 생성.

파서 검증은 Node 스크립트로 pdfjs-dist 좌표 추출 결과를 실제 PDF 원본과 대조하는 방식으로 진행 — 2843 계좌 2025년 매매 75건 전부 수량×단가가 실제 금액과 일치, 배당 7건도 원천세율이 전부 15%(한미 조세조약)로 일관되게 나오는 것까지 확인.

## 5. 실사용 중 발견된 버그들 (사용자 업로드 → 즉시 디버깅)

- **fx_rates 폴백이 무제한이었던 버그**: 2026년 데이터가 fx_rates에 없으니 "이하 가장 가까운 날짜" 로직이 2025-12-31 환율로 조용히 폴백해, 서로 다른 날짜 거래에 전부 같은 환율이 찍히는 문제. 원래 설계 의도(환율 미확보 시 분개 차단)가 폴백 범위 무제한 때문에 무력화된 것 — 폴백 허용 기간을 10일로 제한해서 고침.
- **키움 PDF 두 가지 포맷 문제**: `종합 거래내역 조회`(파서가 다루는 형식)와 `잔고 및 거래 명세서`(완전히 다른 컬럼 구성)를 구분 안 하면 후자를 파싱했을 때 **에러 없이 조용히 0건으로 통과**해버림 — 형식 판별 가드를 추가해 명확한 에러로 바꿈.
- **테스트 잔재 데이터**: 초기 테스트 업로드(스키마 마이그레이션 전) 때 생성된 행 2개가 실거래처럼 검토 화면에 남아있던 것을 실제 원본 CSV와 대조해서 잡아냄.
- **보유종목 개시 잔액 누락**: 신규 업로드분만 로트에 잡혀서 기존 보유 종목이 안 보이던 문제. 세무사 정리 파일로 개시 로트를 복원하는 과정에서 그 파일 자체의 오류(계좌 2845 거래 11건 통째 중복 입력, 종목코드 표기 불일치)까지 발견해서 잡음. 취득원가 재계산 결과, 원본 PDF와 잔고증명서로 교차검증 완료(수량 100% 일치, 원가는 실제 분개 환율과의 미세한 차이인 38,593원만 남아 이후 비례 배분으로 정리).

## 6. 법인세 모듈

사용자가 **"세무사 검토 없이 셀프 신고할 것"**이라고 알려온 뒤 착수. 매도가능증권평가익(기타포괄손익)을 어떻게 세무조정해야 하는지 놓고 한 차례 중요한 자기 정정이 있었다:

1. 처음엔 "익금불산입 △유보 한 줄"로 안내했으나 틀렸음 — 이 평가익은 손익계산서를 거치지 않고 자본으로 직접 계상되므로 당기순이익에 원래 없는데, 거기서 또 빼면 과세표준이 과소계상됨.
2. **실제 2025년 법인세 신고서 원본**(`[프래프트]2025 법인세신고서.pdf`)을 사용자가 제공해 대조한 결과, 올바른 처리는 **두 줄이 짝을 이루는 것**(예: 익금산입 매도가능증권 13,485,958 기타 / 손금산입 매도가능증권평가익 13,485,958 △유보) — 소득 효과가 상계되어 과세표준은 그대로고 유보만 남는 구조. 신고서와 원 단위까지 정확히 일치함을 확인.
3. 유보 이월 로직에서 **전기 평가익 환입분 추인 누락** 버그를 발견 — 매년 초 전기분을 환입하고 기말에 재계상하는 구조라, 환입에 대응하는 유보 추인을 빠뜨리면 유보가 해마다 이중으로 누적됨. "유보 기말잔액 = −(평가익 잔액)"이라는 검증 등식을 화면에 항상 표시하도록 고쳐서 이 종류의 오류를 자동 감지하게 만듦.
4. 선납세금 1,320원을 처음엔 "외국납부세액"(세액공제, 한도 있음)으로 잘못 분류했다가, 신고서의 원천납부세액명세서(갑)를 보고 실제로는 국내 원천징수세액(기납부세액, 한도 없음)임을 확인해 정정.

최종적으로 만든 4개 화면(법인세 계산·소득금액조정합계표·유보 관리·기납부세액과 세액공제)이 2025년 실제 신고 수치(과세표준 533,014 / 산출세액 47,971 / 차감납부할세액 46,771)와 전부 일치하는 것으로 검증 완료.

## 7. 앞으로 남은 것 (이 문서 작성 시점 기준)

- Git 커밋·버전 관리·Vercel 재배포(오늘까지 작업이 전부 미커밋 상태였음)
- 2026년 7월 거래 완전 반영(2843 현금 3건, 2845 증권 5건) 및 반기 마감
- 선납세금 자동분류 매핑 규칙 보강
- 국민은행 `.xls`·키움 PDF 파이프라인의 실제 브라우저 업로드 검증(그동안 CSV만 실사용 검증했음)
- 증권 매도 시 유보 자동 추인 — 종목별 공정가치 이력 추적 구조가 먼저 필요해 별도 설계 예정
- 이월결손금 — 현재는 해당 사항 없음(2025년 설립 이후 계속 흑자)
- 국민은행도 PDF 거래내역을 제공할 수 있다는 점 확인 — `kbBank.js`는 아직 `.xls`만 지원, PDF 파서 추가 여부 검토 필요
