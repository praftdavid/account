# PRAFT 관리회계 시스템 개발 세션 (2026-07-28)

## 1. 다음 목표 점검 + 4가지 지적사항

**사용자 질문**: "이제 뭐해야 하지? 맨 처음에 계획한 첫번째 목표 달성인건가? 그럼 그 다음 목표에서 구현해야 하는 기능은 뭐지?"

기존 로드맵을 검토해 남은 항목(현금흐름표, 표준코드매핑, 급여부기)을 확인. 사용자는 현금흐름표만 우선 진행하기로 하고, 추가로 4가지를 지적:

1. **메뉴 순서 재검토**: 자동분개 하위메뉴의 "증권거래분개 → 증권재평가 → 잔액대사" 순서가 적합한지
2. **증권재평가 기준일 제한 없음**: 반기말/연말 선택지가 없어 아무 날짜나 재평가 가능한 문제
3. **계정별원장 버그**: 2026년을 선택해도 2025년 내역이 다 보이는 문제
4. **다른 기능들의 완전성 검토**

### 처리 결과 (v0.1.6)

- **메뉴 순서**: "증권 재평가"를 "잔액 대사" 뒤로 이동 (주기적 마감 작업은 일상 거래 처리·대사 이후에 오는 게 논리적). `main.js` 상단에 "그룹 내부 정렬은 처리형(업무 순서)·리포트형(요약→상세)으로 다르다"는 원칙 4 추가.
- **계정별원장 버그 수정**: 실질계정(자산·부채·자본)에 "전기이월" 합성 행을 추가하고, 선택 연도 거래만 표시하도록 수정 (`src/pages/ledger.js`).
- **증권재평가 기준일 제한**: 반기말(6/30)·연말(12/31)만 선택 가능한 전용 날짜 선택기(`halfYearEndPickerHtml`, `src/lib/asOfDate.js`) 신규 개발.
- **현금흐름표(간접법) 신규 구현** (`src/pages/cashFlow.js`): 매도가능증권 매매는 영업활동으로 분류(이 회사 실질에 맞춤, AskUserQuestion으로 확정). 개발 중 실제 버그 2건 발견·수정:
  - 매도대금 전액을 현금흐름에 반영하면 당기순이익에 이미 반영된 실현손익이 이중계상됨 → 원가 회수분만 반영
  - 적요 태그(`[증권매수]`/`[증권매도]`) 매칭 방식이 수기 입력 거래를 놓침 → 잔액 기반 공식(Δ매도가능증권 − Δ매도가능증권평가익)으로 재설계
  - 2025·2026 각각 기말현금이 실제 보통예금 잔액과 정확히 일치함을 검증

---

## 2. 후속 요청들 (v0.1.7 ~ v0.1.9)

- **v0.1.7**: 2025년 매매내역도 환전내역과 같은 방식으로 백필 (조회 전용, `status='ignored'`)
- **증권 재평가 탭 질문**: "해당 시점의 평가이익을 볼 수는 없는건가?" → 기존 설계 설명(보유종목현황에서 기준일 선택 시 확인 가능), 코드 변경 없음
- **v0.1.8**: "분개장에 전표번호가 빠졌어" → `entry_no` 자동 채번 기능 추가(`fetchMaxEntryNo`/`formatEntryNo`, `src/lib/data.js`), 기존 29건도 소급 채번

### v0.1.9: 2026년 상반기 금융영업손익 대규모 재계산

**발단**: "금융영업비용은 매도가능증권 매도에 따른 실현손실 반영하는 부분인데, 2025년께 잘 계산 안된 듯" → "올해는 실현손실 없나?" → "내 기억에는 코인베이스 올해 매도하면서 손실 있었던 것 같은데"

**조사 과정**:
- 기존 반기 가결산에 쓰인 "매도실현손익 및 배당금수익(순) 8,900,983원" 단일 합계 전표가 종목별로 검증 불가능했음을 발견
- 원본 PDF 거래내역, DB 저장 전표, CPA 반기 워크시트가 서로 정확히 일치하지 않는다는 것도 발견
- **배당 취소거래 이중계상 버그 발견**: 키움 원본 데이터에 배당이 "최초입금 → 취소출금(같은 금액) → 정정입금" 여러 줄로 나오는데, 적요에 전부 "배당"이 들어있어 취소출금도 잘못 더해지고 있었음 (2026-07-08 배당 4건이 실제 3.28달러인데 9.90달러로 3배 과대계상). `kiwoomPdf.js`/`kiwoomCsv.js`, `buildDividendLines` 수정으로 해결.
- **계정 분리**: 41002를 "증권매매수익"으로 개칭, 41003 "배당금수익" 신설(51002는 "증권매매손실"로 개칭). 손익계산서에 상품매출손익/금융영업손익 구분 표시 추가.
- **9개 종목 전부 재구성**: AXP·테슬라·아이온큐·SOXL·코인베이스·미국채권아이셰어즈ETF·QQQ레버리지ETF 등을 원본 거래내역부터 이동평균으로 재구성. 사용자가 "그냥 890만원은 계산 오류로 판단하는게 맞을 것 같고, 새로 근거를 찾아서 계산한 8,428,152원이... 반영해줘"라고 확정 지시.
- **실행**: entry_66(반기 합계 전표) void 처리 후, 13건의 종목별 개별 전표(7건 매도 P&L + 6건 배당)로 대체. 기존 개별 매도 전표(원가 제거분)와 로트 상태는 그대로 유지(재무상태표·보유종목현황 대사 영향 없음). 최종 확정 금액: **8,359,945원**.

---

## 3. "이어서 하자" — 잔여 작업

- **IONQ 로트 수량 미스터리**: 45주가 맞는지 확인 → 정상(7월 자동분개 파이프라인 매수분 반영 안 됐던 것뿐, 버그 아님)
- **`kiwoomBalanceCert.js` 다중행 종목명 파싱 버그 수정**: 종목명이 길어 PDF 셀 안에서 줄바꿈되는 경우(예: "미국 채권 아이셰어즈" / (데이터 행) / "ETF") 이름이 누락되거나 다음 종목과 뒤섞이는 문제. 두 차례 수정 끝에 "현재 행에 이미 완전한 inlineName이 있으면 다음 행을 흡수하지 않는다"는 조건 추가로 해결. 2025년말(AGG·IAU·IVV, 줄바꿈 있음)과 2026년 상반기(10종목, 줄바꿈 없음) 양쪽 재검증 완료 → **v0.1.10** 배포.

---

## 4. 메뉴 재검토 + 신규 기능 (환율 탭, 대시보드)

### 환율 탭 신규 구현

증권관리 하위메뉴에 "환율" 탭 추가 요청. `fx_rates` 테이블을 조회해, 달러 매수·매도·배당 거래가 있었던 날짜별로 환율이 정확히 확보됐는지(정확/근사/미확보) 보여주는 화면 신규 구현 (`src/pages/securitiesFxRates.js`).

### 대시보드 신규 구현

로그인 후 첫 화면으로 대시보드 신규 개발 (`src/pages/dashboard.js`, 최상위 메뉴로 추가):
- KPI 카드 4개: 자산총계, 현금성자산, 증권평가금액, 당기순이익
- 처음엔 "월별 매출 추이" 차트로 만들었으나, 사용자가 "의미가 없으니 최근 6개 반기별 법인 자산규모 추이(부채+자본 누적막대그래프)로 바꾸자"고 요청 → 반기별 자산구조 스택 막대 차트로 교체. 재무상태표와 동일한 로직(마감분개 없이 누적순이익을 미처분이익잉여금에 얹는 방식)으로 정확히 대차가 맞도록 계산.
- 제목을 "자산규모 추이 (최근 3개년)"으로 재수정
- "계좌별 현금비중"은 의도적으로 넣지 않음 — 국민은행·키움2843·키움2845 실물계좌 3개가 전부 같은 GL계정(11101 보통예금)에 연결돼 있어 원장 데이터만으로는 나눌 수 없기 때문(각주로 이유 명시).

**미리보기 배포 후 사용자 승인 대기 중** ("결과물을 내가 승인하면 그게 v0.2가 되는거야").

---

## 5. 사이드 채팅 — 2026년 상반기 데이터 정합성 긴급 이슈

사용자가 다른 세션(사이드 채팅)에서 나눈 대화를 공유하며 지적:

> "2026년 환전 거래가 이렇게 여러번 있었나? 환율 자료가 왜이렇게 많아?"
> "6월 이전에는 왜 거래가 없었지? 이 때도 매수 매도 거래 있었는데."

**진단**: 환율 탭(및 매매내역·배당금내역)은 `securities_transactions` 테이블만 조회하는데, 2026년 상반기(1~6월) 매수·매도·배당 거래가 이 테이블에 전혀 없었음. v0.1.9의 반기 손익 재구성 작업 때 `journal_entries`에만 직접 반영하고 `securities_transactions`는 건드리지 않았기 때문.

**최종 지시**: "우리가 검증 작업하면서 자동분개 파이프라인 거치지 않은 거래는 나중에도 오류가 없도록 정당처리해줘"

### 조사 확대 및 실제 위험 발견

단순 표시 공백인 줄 알았으나, 조사 결과 **실제 이중분개 위험**을 발견:
- `importTransactions.js`의 업로드 파이프라인은 `securities_transactions.dedup_key` 유니크 제약으로 중복 방지를 함
- 2026년 상반기 데이터가 이 테이블에 전혀 없다는 것은, 향후 원본 상반기 거래내역 파일이 실수로라도 재업로드되면 dedup 인식 없이 그대로 재분개될 위험이 있다는 뜻

### 백필 작업 (52건)

- **매수 35건 + 매도 11건 + 배당 6건**을 `securities_transactions`에 `status='ignored'`로 백필 (2025년 백필과 동일 패턴, 재무제표에는 영향 없음)
- 매도 11건 중 7건은 이전 정정 시도(void 처리된 전표)에 남아있던 실제 USD 단가를 그대로 사용, 나머지 4건(AGG 3건·QQQ 1건)은 실제 원화 매도대금을 환율로 역산
- 종목코드(GOOGL·IONQ·MRVL·QQQ 등)를 `securities_valuations` 기존 데이터와 대조해 정확히 매칭
- **매수 수량을 2026-06-30 잔고증명서(이번 세션에서 고친 파서로 재파싱)와 대조 → 7개 종목 전부 정확히 일치 확인**
- fx_rates 누락 2건(05-27, 06-02)도 분개에 실제 기재된 환율로 보강해 환율 탭 미확보 해소
- 환율 탭 이름을 "환율(거래일 대사)"로 변경해 "환전 거래"로 오해되지 않도록 개선

### 부수 확인 사항

- **증권재평가 PDF 복수 업로드**: 이미 지원됨 확인(`<input type="file" ... multiple>`, 여러 잔고증명서 한 번에 매칭 가능)

---

## 6. 마지막 대화 — 추가 지적사항 (미완료)

사용자가 마지막으로 2가지를 지적:

1. **증권재평가 탭**: 주식 매수·매도가 계속될 텐데, 새 종목 추가나 전량 매도 시 삭제가 제대로 되는지 궁금 (조사 시작: `securitiesReview.js`에서 `securities_lots`는 신규 매수 시 upsert로 생성, 전량 매도 시 `status: 'closed'`로 전환되는 로직 확인 중이었음 — **답변 미완료**)
2. **매매내역 탭**: 배당금내역·환전내역·환율 탭과 달리 연도별 분류 기능이 없음. "같은 성격의 화면은 말 안 해도 검토해서 적용해야지"라는 피드백 — **아직 미반영**

---

## 배포 이력

| 버전 | 내용 |
|---|---|
| v0.1.6 | 메뉴 재검토, 계정별원장 버그 수정, 재평가 기준일 제한, 현금흐름표 신규 |
| v0.1.7 | 2025년 매매내역 백필 |
| v0.1.8 | 전표번호 자동 채번 |
| v0.1.9 | 2026 상반기 금융영업손익 재계산 + 배당 취소거래 버그 수정 |
| v0.1.10 | 잔고증명서 종목명 다중행 파싱 버그 수정 |
| (미배포) | 환율 탭, 대시보드, 2026 상반기 securities_transactions 백필 — 사용자 승인 대기 중 |
