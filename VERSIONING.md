# 버전 관리 기준

이 프로젝트는 **0.x를 "아직 완성되지 않은 초안" 구간**으로 쓴다. 일반적인 시맨틱 버저닝과 숫자 규칙은 같지만 의미가 다르다.

## 규칙

| 버전대 | 의미 |
|---|---|
| `0.x.y` | **아직 미완성.** 핵심 기능이 갖춰지는 중이며, 언제든 구조가 바뀔 수 있음. 우리가 지금 이 단계. |
| `1.0.0` | **최초 완성판.** "이 정도면 회사 회계 업무를 이걸로 돌려도 된다"고 판단되는 시점에 붙인다. |
| `1.x.0` | 완성 이후의 마이너 업데이트(기능 추가). |
| `1.x.y` | 완성 이후의 패치(버그 수정). |
| `2.0.0` | 완성 이후 구조가 크게 바뀌는 큰 개편(예: 스키마 전면 재설계). |

즉 `1.0.0`이 찍히기 전까지는 전부 "초안 단계"의 마이너 수정으로 취급한다 — `0.1` → `0.2` → `0.3`처럼 올라가다가, 실사용 가능하다고 판단되는 시점에 `1.0.0`으로 넘어간다.

## 버전을 올리는 시점

기능 단위로 의미 있는 작업 묶음이 끝났을 때 사용자(대표님)와 상의해서 버전을 올린다. 예:
- 증권 자동분개 파이프라인 전체 완성 → `0.1.0` → `0.2.0`
- 법인세 계산 4개 화면 추가 → `0.2.0` → `0.3.0`
- 오탈자·소소한 버그 수정 → `0.2.0` → `0.2.1`

## 실제로 하는 일

1. `package.json`의 `"version"` 필드를 갱신
2. `CHANGELOG.md`에 해당 버전 항목 추가(무엇이 바뀌었는지 한글로 요약)
3. `git commit` 후 `git tag v0.2.0` 형태로 태그
4. `vercel --prod`로 배포

## 참고

- Vercel은 git 연동이 아니라 CLI(`vercel --prod`)로 직접 배포하는 방식이라, 버전을 올렸다고 자동으로 배포되지 않는다 — 태그를 찍은 뒤 반드시 `vercel --prod`를 별도로 실행해야 실제 서비스에 반영된다.
- git 원격 저장소(GitHub 등)는 아직 없다. 로컬 저장소 + 태그만으로 버전 이력을 관리한다.
